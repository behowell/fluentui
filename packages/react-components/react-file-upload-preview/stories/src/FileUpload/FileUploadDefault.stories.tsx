import * as React from 'react';
import { FileUpload, FileUploadProps } from '@fluentui/react-file-upload-preview';

export const Default = (props: Partial<FileUploadProps>) => <FileUpload {...props} />;
